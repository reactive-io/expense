/* global angular */

(function() {
  "use strict";

  var mod = angular.module('directives').directive('myNavigationLink',
  ['$location', function($location) {

    return {
      restrict: 'E',

      scope: true,
      replace: true,

      templateUrl: '<%= asset_path('directives/navigation/link.html') %>',

      link: function($scope, element, attrs) {
        $scope.attrs = attrs;

        $scope.attrs.isActive = function() {
          return attrs.href === $location.path();
        };
      }
    }
  }]);
})();
